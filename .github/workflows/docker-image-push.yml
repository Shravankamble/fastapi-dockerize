name: Docker Image CI

on:
  push:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Build the Fastapi Docker image
      run: |
        docker build . --file Dockerfile --tag fastapi-dockerize:$(date +%s)
